//string

let name = 'Rohit';

//int , float
let age = 25.5;

// boolean
let isPaid = true;

// null tempp ke bare mein socho
let favClass = null;

// baad mein dekhenge
let undef;

let skills = ['html', 'javascript', 'CSS'];
console.log(skills);

let student_profile = {
  // direct memory ka address like storing c++ ke
  //variable references bhi store ho sakte hain like bridges
  // key val pairs
  name: 'haseeb',
  age: 22,
  isPaid: true,
  skills: ['html', 'javascript', 'CSS'],
  favClass: null,
};

// array , object , symbol(during react) , bigInt

// primitive and non primitive(objects) data types
console.log(typeof isPaid);
